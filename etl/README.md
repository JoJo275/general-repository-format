# ETL (Extract, Transform, Load)

This folder is where a business might place scripts that:

- Import data from CSV files or external systems (Extract).
- Clean or reshape that data (Transform).
- Insert it into this database (Load).

We include a small, commented Python example to show the idea.
