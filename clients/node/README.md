# Node.js Client (Placeholder)

In a real business repo, this folder might contain JavaScript/TypeScript
code that talks to the database using a library like `pg` (Postgres),
`mysql2` (MySQL), or `better-sqlite3` (SQLite).

We only include this README to show where that code would live.
